var numbers = [1, 2, 3, 4, 5];

/**
 * Return a new array depend on given condition
 */

let result = numbers.map((num) => {
  //   return num;
  //   return 2 * num;
  return num > 2;
});

console.log(result);
